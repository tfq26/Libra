{
  "name": "libra",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "cd Frontend && bun run dev",
    "start:backend": "cd Backend && npm install && func start",
    "install:all": "npm install -g concurrently && cd frontend && bun install && cd ../Backend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "vite": "^7.1.7"
  },
  "workspaces": [
    "frontend",
    "Backend"
  ]
}
